apiVersion: 1

datasources:
  - name: ServiceOps Prometheus
    uid: serviceops-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
